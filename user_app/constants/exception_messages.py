INVALID_OFFSET_LENGTH = (
    'Offset should be greater than or equal to zero and length should be '
    'greater than zero', 'INVALID_OFFSET_LENGTH'
)

INVALID_USER_ID = (
    "Invalid user_id, try with valid user_id",
    "INVALID_USER_ID"
)

INVALID_USERNAME = (
    "Invalid user name, try with valid user name",
    "INVALID_USERNAME"
)

INVALID_PASSWORD = (
    "Invalid password, try with valid password",
    "INVALID_PASSWORD"
)

INVALID_WORKFLOW = (
    "Invalid workflow, try with valid workflow",
    "INVALID_WORKFLOW"
)

INVALID_ACCESS_TOKEN = (
    "Invalid access_token",
    "INVALID_ACCESS_TOKEN"
)

INVALID_PROJECT = (
     "Invalid project_id, try with valid project_id",
     "INVALID_PROJECT"
    )

INVALID_STATE = (
     "Invalid state_id, try with valid state_id",
     "INVALID_STATE"
    )

INVALID_TASK = (
     "Invalid Task, try with valid task",
     "INVALID_TASK"
    )

INVALID_TRANSITION = (
     "Invalid Transition, try with valid transition",
     "INVALID_TRANSITION"
    )

UN_AUTHORIZED = (
     "User not allowed to access this resource",
     "UN_AUTHORIZED"
    )

UN_AUTHORIZED_DEVELOPER =  (
     "Developer not allowed to access this resource",
     "UN_AUTHORIZED"
    )

CHECKLIST_NOT_SATISFIED = (
     "Check list mandatory fields not checked",
     "CHECKLIST_NOT_SATISFIED"
    )

INVALID_LIMIT =  (
    "Invalid limit value in query , try with valid values",
    "INVALID_LIMIT")
INVALID_OFFSET = (
    "Invalid offset value in query , try with valid values",
    "INVALID_OFFSET")
